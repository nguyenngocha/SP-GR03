<%= render 'layouts/tool' %>
	<div class="row">
		<% if @products.any? %>
			<%= render @products %>
			<% else %>
			<%= @text ="<h3>Không có sản phẩm</h3>".html_safe %>
			<% end %>
	</div>
<%= will_paginate :previous_label => 'Trước', :next_label => 'Tiếp' %>
