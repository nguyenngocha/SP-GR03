<%= render 'layouts/tool' %>
	<div class="row">
		<% if @products.any? %>
			<%= render @products %>
		<%= will_paginate%>			
			<% else %>
			<%= @text ="<h3>Không có sản phẩm</h3>".html_safe %>
			<% end %>
	</div>
